-- starter_code is the function stub shown to the user in the code editor
-- (e.g. "class Solution:\n    def twoSum(...):"). Separate from prompt, which
-- is the hidden eval boilerplate (imports, helper classes) used by the worker.
ALTER TABLE problems ADD COLUMN starter_code TEXT;
